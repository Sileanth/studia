
module Max = functor(M : sig
    type this
    val compare : this -> this -> Ord
  end) =>
struct
  let max x y =
    match M.compare x y with
    | LT => y
    | _  => x
    end
end : sig
  val max : M -> M -> M
end
